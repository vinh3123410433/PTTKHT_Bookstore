{{#block "css"}}
  <link rel="stylesheet" href="/css/warehouse/category.css" />
{{/block}}

{{#block "js"}}
  <script src="/js/warehouse/category.js"></script>
{{/block}}

<!-- Category table -->
<div class="category-table">
  <div class="inside-table">
    <div class="categoryHeader">
      <h2>Danh Mục</h2>
      {{#if (or (includes action "create") (includes action "all"))}}

        <div>
          <a href="/admin/warehouse/category/create"><button
              class="add-category"
            >Thêm Danh Mục</button></a>
        </div>
      {{/if}}
    </div>
    <table>
      <thead>
        <tr>
          <td>ID danh mục</td>
          <td>Tên danh mục</td>
          <td>Chức năng</td>
        </tr>
      </thead>
      <tbody class="category-list">
        {{#each category}}
          <tr>
            <td>{{this.DanhMucID}}</td>
            <td>{{this.TenDanhMuc}}</td>
            <td>
              <div class="button-container">
                <a href="/admin/warehouse/category/view/{{this.DanhMucID}}">
                  <button class="icon-btn view-btn">
                    <i class="fas fa-eye"></i>
                    <span class="tooltip">Chi tiết</span>
                  </button>
                </a>
                
                  {{#if
                    (or (includes ../action "edit") (includes ../action "all"))
                  }}
    
                <a href="/admin/warehouse/category/update/{{this.DanhMucID}}">
                  <button class="icon-btn edit-btn">
                    <i class="fas fa-edit"></i>
                    <span class="tooltip">Chỉnh sửa</span>
                  </button>
                </a>
                {{/if}}

                
                  {{#if
                    (or (includes ../action "delete") (includes ../action "all"))
                  }}

                <button
                  class="icon-btn delete-btn"
                  data-id="{{this.DanhMucID}}"
                >
                  <i class="fas fa-trash-alt"></i>
                  <span class="tooltip">Xóa</span>
                </button>
                {{/if}}
              </div>
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>

<!-- Delete product -->
<div class="delete-popup">
  <div class="popup">
    <div class="popup-head">
      <p>Bạn có chắc chắn muốn xóa danh mục?</p>
      <button class="popup-close-btn"><i class="fas fa-times"></i></button>
    </div>
    <div class="popup-body">
      <p>Nếu đã xóa, bạn sẽ không thể tiếp tục sử dụng danh mục.</p>
    </div>
    <div class="popup-foot">
      <button class="popup-btn popup-cancel-btn">Hủy</button>
      <button class="popup-btn popup-confirm-btn">Xác nhận</button>
    </div>
  </div>
</div>